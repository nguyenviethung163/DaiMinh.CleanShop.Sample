# Architecture Documentation

Start with [Architecture Overview](overview.md), then [Dependency Rules](dependency-rules.md), [Domain Model](domain-model.md), [Application Model](application-model.md), [Persistence](persistence.md), and [Request Flows](request-flow.md).

These documents describe the **current accepted architecture**. Historical reasoning belongs in `../decisions/`.
